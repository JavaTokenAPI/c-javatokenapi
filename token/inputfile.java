import.os
import.stream
import.element
     INIT == MAIN.CLASS
      @Ovveride
        stream(init.soicalView)
       init.soical( ==> file.git
     MAIN == CLASS STRUCTURE
        init.element
      socialView.git
   
        
